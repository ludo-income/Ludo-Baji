Ludo Baji Admin Panel V3

Render settings:
Build Command: npm install
Start Command: npm start

Admin URL: /admin
Username: admin
Password: admin123

This version fixes the login handler and uses the server API for shared state.
The main page refreshes its state automatically every 3 seconds, so Admin changes appear without manually rebuilding the site.
